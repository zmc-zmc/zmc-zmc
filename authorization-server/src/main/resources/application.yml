
server:
  port: 9000
spring:
  application:
    name: = authorization-server
# H2 properties
  datasource:
    url: jdbc:mysql://localhost:3306/zmc?useSSL=false
    driver-class-name: com.mysql.jdbc.Driver 
    username: root
    password: root
    
  #h2:
    #console:
     # enabled: true
      #path: /h2-console
  jpa:
    generate-ddl : true
    show-sql: true
    properties:
      hibernate :
        dialect: org.hibernate.dialect.MySQL5Dialect   

